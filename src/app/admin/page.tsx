import { AdminEditor } from "./admin-editor";

export const metadata = {
  title: "Admin | My Lead Partner",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminEditor />;
}
